---
- name: Bootstrap ceph cluster on the admin node
  hosts: admin
  become: true
  gather_facts: false
  vars_files:
    - "{{ playbook_dir }}/../inventory/topologies/{{ ceph_topology }}.yml"
  roles:
    - cephadm_bootstrap
