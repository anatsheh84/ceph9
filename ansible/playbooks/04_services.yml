---
- name: Deploy CephFS, RGW, NFS, RBD pool
  hosts: admin
  become: true
  gather_facts: false
  vars_files:
    - "{{ playbook_dir }}/../inventory/topologies/{{ ceph_topology }}.yml"
  roles:
    - ceph_services
